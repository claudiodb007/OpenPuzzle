# Engines

Engines perform the actual keyspace search.

Planned engines: BitCrack, KeyHunt, Kangaroo, VanitySearch and future plugins.

All engines should eventually implement a common interface.

The OpenPuzzle core should not contain engine-specific scheduling logic.
