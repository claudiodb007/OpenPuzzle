[Unit]
Description=OpenPuzzle continuous client (%i backend)
Documentation=https://github.com/claudiodb007/OpenPuzzle
Wants=network-online.target
After=network-online.target
StartLimitIntervalSec=300
StartLimitBurst=5

[Service]
Type=simple
Environment=OPENPUZZLE_EXECUTION_SLOT=primary
Environment=OPENPUZZLE_SERVICE_ARGS=
EnvironmentFile=-%h/.config/OpenPuzzle/%i.env
ExecStart=/usr/bin/openpuzzle run --backend %i $OPENPUZZLE_SERVICE_ARGS
ExecStop=/usr/bin/openpuzzle stop
Restart=on-failure
RestartSec=30s
TimeoutStopSec=180s
KillMode=mixed
UMask=0077
WorkingDirectory=%h
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=default.target
